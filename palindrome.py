#! usr/bin/env python3

palindrome = str(input("Enter a string value: "))
length = int(len(palindrome))
f_half = palindrome[:(length/2) -1]
l_half = palindrome[length/2:]

x = -1
for i in t,s:
    if t[i] != s[x]:
        print("Not a palindrome")
        break
    elif t
    x = -1